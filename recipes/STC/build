#!/bin/sh -e
meson setup build \
    --prefix="$CHY_PREFIX" \
    --sysconfdir="$CHY_ROOT/etc" \
    --libdir=lib \
    --buildtype=release \
    -Db_lto=false \
    -Dexamples=disabled
ninja -C build
DESTDIR="$1" ninja -C build install
